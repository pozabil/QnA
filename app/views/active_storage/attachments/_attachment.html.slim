div id="attachment-#{attachment.id}"
  = link_to short_filename(attachment), url_for(attachment)
  - if attachment.record.user == current_user
    span class='remove-attachment-link' style="display: none;"
      = ' | '
      = remove_attachment(attachment)